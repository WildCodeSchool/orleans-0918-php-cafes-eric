{% extends 'layoutAdmin.html.twig' %}

{% block title %}Liste des infusions{% endblock %}

{% block body %}
    <div class="container">
        <h1>Liste des infusions</h1>
        <div class="mb-5 mt-5">
            <a class="btn btn-outline-dark" href="{{ path('infusion_new') }}"><i
                        class="fa fa-plus"></i> Ajouter une nouvelle infusion</a>
        </div>

        <table class="table">
            <thead>
            <tr>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for infusion in infusions %}
                <tr>
                    <td class="col-md-9"><i class="fas fa-leaf"></i> {{ infusion.name }}</td>
                    <td class="col-md-3">
                        <a class="btn btn-outline-dark"
                           href="{{ path('infusion_show', {'id': infusion.id}) }}"><i class="far fa-eye"></i> Voir</a>
                        <a class="btn btn-outline-dark"
                           href="{{ path('infusion_edit', {'id': infusion.id}) }}"><i class="fas fa-pencil-alt"></i>
                            Éditer</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7">Liste vide</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
