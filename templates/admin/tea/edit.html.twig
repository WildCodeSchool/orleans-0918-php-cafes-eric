{% extends 'layoutAdmin.html.twig' %}

{% block title %}Éditer un thé{% endblock %}

{% block body %}
    <h1 class="admin-title">Éditer un thé</h1>
<div class="row">
    <div class="col-md-12 mb-5 mt-5">
        {{ include('admin/tea/_form.html.twig', {'button_label': 'Éditer'}) }}
    </div>
    <div class="col-md-12">
        <a class="btn btn-outline-dark" href="{{ path('tea_index') }}"><i class="fas fa-reply"></i> Retour</a>
    </div>
</div>
{% endblock %}
