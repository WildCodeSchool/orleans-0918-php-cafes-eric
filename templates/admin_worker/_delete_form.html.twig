<form class="delete_form" method="post" action="{{ path('worker_delete', {'id': worker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ worker.id) }}">
    <button class="btn btn-outline-secondary">Effacer</button>
</form>
