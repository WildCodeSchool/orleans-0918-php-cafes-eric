{% extends 'layout.html.twig' %}
{% block title %}Nos infusions{% endblock %}
{% block body %}
<section class="page-section clearfix">
    <div class="container">
        <div class="about-heading-content">
            <div class="bg-faded rounded p-2 mb-5">
                <h1 class="section-heading">
                    <span class="section-heading-lower justify-content-center d-flex ">Infusions</span>
                </h1>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="intro-infusion">
            <img class="intro-img img-fluid mb-3 mb-lg-0 rounded" src="{{ asset('build/infusionImages/infusionbg.jpg') }}" alt="thé">
        </div>
    </div>
</section>
<section>
    <div class="container">
        <div class="row justify-content-center ">
            {% for category in categories %}
                <div class="col-md-12">
                    <div class="bg-faded rounded p-2 mb-5">
                        <h2 class="linkCoffee section-heading-lower area d-flex justify-content-center ">
                            <a href="{{ path('infusion_category_show', {'id': category.id }) }}">{{ category.title }}</a>
                        </h2>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</section>
{% endblock %}


